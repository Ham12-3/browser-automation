document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("root");
  if (root) {
    root.textContent = "OmniBrowse bridge is visible and permission-based.";
  }
});

